import React from 'react';
import PasswordToken from '../../components/organisms/PasswordToken';

const Token = () => {
  return (
    <>
      <PasswordToken />
    </>
  );
};

export default Token;
